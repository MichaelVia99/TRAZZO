<UserControl x:Class="Bitacora.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Bitacora.Models"
             Background="Transparent">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TabActiveBrush" Color="#E65100"/>
        <SolidColorBrush x:Key="TabInactiveBrush" Color="#666666"/>
        <SolidColorBrush x:Key="TabActiveTextBrush" Color="White"/>
    </UserControl.Resources>
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="Transparent"
                Margin="15"
                BorderBrush="{StaticResource CardBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="TitleTextBlock"
                               Text="Panel"
                               FontSize="13" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock x:Name="SubtitleTextBlock"
                               Text=""
                               FontSize="10" 
                               Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button x:Name="CerrarSesionButton"
                            Click="LogoutButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Padding="6,4"
                            Margin="0,0,4,0">
                        <TextBlock Text="Cerrar Sesión"
                                   FontSize="10"
                                   Foreground="{StaticResource TextSecondary}"/>
                    </Button>
                    <Button x:Name="CerrarAplicacionButton"
                            Click="CerrarAplicacionButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Padding="6,4">
                        <TextBlock Text="Salir"
                                   FontSize="10"
                                   Foreground="{StaticResource TextSecondary}"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Tabs de navegación -->
        <Border Grid.Row="1"
                Background="Transparent"
                Margin="12,0"
                BorderBrush="{StaticResource CardBorder}"
                BorderThickness="0,0,0,1">
            <Border.Resources>
                <Style x:Key="TabButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="16,6"/>
                    <Setter Property="Foreground" Value="{StaticResource TabInactiveBrush}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Grid Background="Transparent">
                                    <StackPanel>
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"/>
                                        <Border x:Name="Indicator"
                                                Height="2"
                                                Background="{StaticResource TabActiveBrush}"
                                                VerticalAlignment="Bottom"
                                                Visibility="Hidden"
                                                CornerRadius="1"/>
                                    </StackPanel>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource TabActiveBrush}"/>
                                    </Trigger>
                                    <Trigger Property="Tag" Value="Active">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Border.Resources>
            
            <StackPanel Orientation="Horizontal">
                <!-- Tab Dashboard -->
                <Button x:Name="DashboardTabButton"
                        Content="Dashboard"
                        Style="{StaticResource TabButtonStyle}"
                        Click="DashboardTabButton_Click"
                        Tag="Inactive"
                        Margin="0,0,8,0"/>
                
                <!-- Tab Solicitudes (solo Gestor) -->
                <Button x:Name="SolicitudesTabButton"
                        Content="Solicitudes"
                        Style="{StaticResource TabButtonStyle}"
                        Click="SolicitudesTabButton_Click"
                        Tag="Inactive"
                        Margin="0,0,8,0"/>
                
                <!-- Tab Asignaciones -->
                <Button x:Name="AsignacionesTabButton"
                        Content="Asignaciones"
                        Style="{StaticResource TabButtonStyle}"
                        Click="AsignacionesTabButton_Click"
                        Tag="Active"/>
            </StackPanel>
        </Border>
        
        <!-- Área de contenido -->
        <ContentControl x:Name="ContentArea" Grid.Row="2" Margin="10,5,15,0" />
    </Grid>
</UserControl>
