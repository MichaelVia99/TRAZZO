<Application x:Class="Bitacora.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Bitacora.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Convertidores -->
        <converters:StringToBooleanConverter x:Key="StringToBooleanConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:PathToFileNameConverter x:Key="PathToFileNameConverter"/>
        <converters:PathToIsImageConverter x:Key="PathToIsImageConverter"/>
        <converters:PathTokenToThumbnailConverter x:Key="PathTokenToThumbnailConverter"/>
        <converters:PathTokenToSizeMbConverter x:Key="PathTokenToSizeMbConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Colores Base -->
        <Color x:Key="PrimaryOrangeColor">#E65100</Color>
        <Color x:Key="PrimaryOrangeLightColor">#FF9800</Color>
        <Color x:Key="PrimaryOrangeDarkColor">#BF360C</Color>
        <Color x:Key="AlertRedColor">#C0392B</Color>
        <Color x:Key="SuccessGreenColor">#27AE60</Color>
        <Color x:Key="InfoBlueColor">#3498DB</Color>
        <Color x:Key="ProcessOrangeColor">#F39C12</Color>
        <Color x:Key="NewButtonRedColor">#E74C3C</Color>
        <Color x:Key="CardGrayColor">#F5F5F5</Color>
        <Color x:Key="StatusPlanificarColor">#7E57C2</Color>
        <Color x:Key="StatusEnEsperaColor">#FBC02D</Color>
        <Color x:Key="StatusEnCursoColor">#00BCD4</Color>
        <Color x:Key="StatusPausadoColor">#607D8B</Color>
        <Color x:Key="StatusCompletadoColor">#4CAF50</Color>
        <Color x:Key="StatusTodosColor">#57534D</Color>
        <Color x:Key="NeutralDarkColor">#333333</Color>
        <Color x:Key="NeutralMediumColor">#888888</Color>
        <Color x:Key="NeutralBorderColor">#E0E0E0</Color>
        <Color x:Key="PriorityCriticalColor">#D32F2F</Color>
        <Color x:Key="PriorityHighColor">#F57C00</Color>
        <Color x:Key="PriorityNormalColor">#1976D2</Color>
        <Color x:Key="PriorityLowColor">#388E3C</Color>
        <Color x:Key="PathLinkColor">#64748B</Color>
        <Color x:Key="SecundaryButtonBackgroundColor">#1a888888</Color>

        <!-- Pinceles (Brushes) -->
        <SolidColorBrush x:Key="PrimaryOrangeBrush" Color="{StaticResource PrimaryOrangeColor}"/>
        <SolidColorBrush x:Key="PrimaryOrangeLightBrush" Color="{StaticResource PrimaryOrangeLightColor}"/>
        <SolidColorBrush x:Key="OrangeDarkBrush" Color="{StaticResource PrimaryOrangeDarkColor}"/>
        <SolidColorBrush x:Key="AlertRedBrush" Color="{StaticResource AlertRedColor}"/>
        <SolidColorBrush x:Key="SuccessGreenBrush" Color="{StaticResource SuccessGreenColor}"/>
        <SolidColorBrush x:Key="InfoBlueBrush" Color="{StaticResource InfoBlueColor}"/>
        <SolidColorBrush x:Key="WarningOrangeBrush" Color="{StaticResource PrimaryOrangeLightColor}"/>
        <SolidColorBrush x:Key="ProcessOrangeBrush" Color="{StaticResource ProcessOrangeColor}"/>
        <SolidColorBrush x:Key="NewButtonRedBrush" Color="{StaticResource NewButtonRedColor}"/>
        <SolidColorBrush x:Key="CardGrayBrush" Color="{StaticResource CardGrayColor}"/>
        <SolidColorBrush x:Key="TextDarkBrush" Color="#202020"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#666666"/>
        <SolidColorBrush x:Key="TextWhiteBrush" Color="White"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="{StaticResource NeutralBorderColor}"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="{StaticResource NeutralBorderColor}"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#202020"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#666666"/>
        <SolidColorBrush x:Key="OrangeBrush" Color="{StaticResource PrimaryOrangeColor}"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#2E7D32"/>
        <SolidColorBrush x:Key="GlassBackground" Color="#F2FFFFFF"/>
        <SolidColorBrush x:Key="GlassBorder" Color="#1AFFFFFF"/>
        <SolidColorBrush x:Key="TabActiveBrush" Color="{StaticResource PrimaryOrangeColor}"/>
        <SolidColorBrush x:Key="TabInactiveBrush" Color="#666666"/>
        <SolidColorBrush x:Key="ContentBackground" Color="Transparent"/>
        <SolidColorBrush x:Key="StatusPlanificarBrush" Color="{StaticResource StatusPlanificarColor}"/>
        <SolidColorBrush x:Key="StatusEnEsperaBrush" Color="{StaticResource StatusEnEsperaColor}"/>
        <SolidColorBrush x:Key="StatusEnCursoBrush" Color="{StaticResource StatusEnCursoColor}"/>
        <SolidColorBrush x:Key="StatusPausadoBrush" Color="{StaticResource StatusPausadoColor}"/>
        <SolidColorBrush x:Key="StatusCompletadoBrush" Color="{StaticResource StatusCompletadoColor}"/>
        <SolidColorBrush x:Key="StatusTodosBrush" Color="{StaticResource StatusTodosColor}"/>
        <SolidColorBrush x:Key="NeutralDarkBrush" Color="{StaticResource NeutralDarkColor}"/>
        <SolidColorBrush x:Key="NeutralMediumBrush" Color="{StaticResource NeutralMediumColor}"/>
        <SolidColorBrush x:Key="NeutralBorderBrush" Color="{StaticResource NeutralBorderColor}"/>
        <SolidColorBrush x:Key="PriorityCriticalBrush" Color="{StaticResource PriorityCriticalColor}"/>
        <SolidColorBrush x:Key="PriorityHighBrush" Color="{StaticResource PriorityHighColor}"/>
        <SolidColorBrush x:Key="PriorityNormalBrush" Color="{StaticResource PriorityNormalColor}"/>
        <SolidColorBrush x:Key="PriorityLowBrush" Color="{StaticResource PriorityLowColor}"/>
        <SolidColorBrush x:Key="PathLink" Color="{StaticResource PathLinkColor}"/>
        <SolidColorBrush x:Key="SecundaryButtonBackground" Color="{StaticResource SecundaryButtonBackgroundColor}"/>
        
        <DropShadowEffect x:Key="CardShadow" 
                          BlurRadius="12" 
                          ShadowDepth="2" 
                          Direction="10"
                          Color="Black" 
                          Opacity="0.12"/>
        <DropShadowEffect x:Key="ElevatedShadow" 
                          BlurRadius="24" 
                          ShadowDepth="4" 
                          Direction="270"
                          Color="Black" 
                          Opacity="0.12"/>
        <DropShadowEffect x:Key="MaterialShadow" 
                          BlurRadius="20" 
                          ShadowDepth="2" 
                          Direction="270"
                          Color="Black" 
                          Opacity="0.3"/>
        
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="{StaticResource PrimaryOrangeLightColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource PrimaryOrangeColor}" Offset="1.0"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PrimaryGradientVertical" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="{StaticResource PrimaryOrangeLightColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource PrimaryOrangeColor}" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- Estilos Globales -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>

        <!-- Estilo para TextBox Redondeado -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Padding" Value="3,2"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="Gray"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="5" Opacity="0.1" Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para PasswordBox Redondeado -->
        <Style x:Key="RoundedPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Padding" Value="3,2"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="Gray"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="5" Opacity="0.1" Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para Botón Principal -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryOrangeBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryOrangeLightBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="6" Opacity="0.2" Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20E0E0E0"/>
                                <Setter Property="Foreground" Value="{StaticResource OrangeBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ContentContainerStyle" TargetType="ContentControl">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border Background="{StaticResource GlassBackground}"
                                BorderBrush="{StaticResource GlassBorder}"
                                BorderThickness="1"
                                CornerRadius="5"
                                Padding="8"
                                Effect="{StaticResource ElevatedShadow}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
